{
  "name": "@flowdoc/vnext-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Extractable FlowDoc vNext workspace for the next document model.",
  "scripts": {
    "type-check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts",
    "check": "npm run type-check && npm run test"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
